"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.buildExternalCliPrompt = buildExternalCliPrompt;exports.runExternalCli = runExternalCli;var _nodeChild_process = await jitiImport("node:child_process");
var fs = _interopRequireWildcard(await jitiImport("node:fs"));
var path = _interopRequireWildcard(await jitiImport("node:path"));
var _promises = await jitiImport("node:stream/promises");
var _postExitStdioGuard = await jitiImport("../../shared/post-exit-stdio-guard.ts");function _interopRequireWildcard(e, t) {if ("function" == typeof WeakMap) var r = new WeakMap(),n = new WeakMap();return (_interopRequireWildcard = function (e, t) {if (!t && e && e.__esModule) return e;var o,i,f = { __proto__: null, default: e };if (null === e || "object" != typeof e && "function" != typeof e) return f;if (o = t ? n : r) {if (o.has(e)) return o.get(e);o.set(e, f);}for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]);return f;})(e, t);}


const HARD_KILL_DELAY_MS = 2_000;
const MAX_OUTPUT_TAIL_BYTES = 64 * 1024;
const MAX_ERROR_TAIL_BYTES = 64 * 1024;

function buildExternalCliPrompt(systemInstructions, task) {
  return `<System instructions>\n${systemInstructions.trim()}\n\n<Task>\n${task}`;
}











function runExternalCli(input)











{
  return new Promise((resolve, reject) => {
    const startedAt = Date.now();
    const stdoutPath = path.join(input.asyncDir, `external-${input.stepIndex}.stdout.log`);
    const stderrPath = path.join(input.asyncDir, `external-${input.stepIndex}.stderr.log`);
    fs.mkdirSync(input.asyncDir, { recursive: true });
    const stdoutStream = fs.createWriteStream(stdoutPath, { flags: "w" });
    const stderrStream = fs.createWriteStream(stderrPath, { flags: "w" });
    const streamsFinished = Promise.allSettled([(0, _promises.finished)(stdoutStream), (0, _promises.finished)(stderrStream)]);
    let stdoutTail = Buffer.alloc(0);
    let stderrTail = Buffer.alloc(0);
    let timedOut = false;
    let stopped = false;
    let settled = false;
    let hardKillTimer;
    const child = (0, _nodeChild_process.spawn)(input.command, input.args ?? [], {
      cwd: input.cwd,
      stdio: ["pipe", "pipe", "pipe"],
      shell: false,
      windowsHide: true
    });
    const initialProcess = {
      ...(typeof child.pid === "number" ? { pid: child.pid } : {}),
      startedAt,
      stdoutPath,
      stderrPath
    };
    input.onProcess?.(initialProcess);
    child.stdout.on("data", (chunk) => {
      stdoutStream.write(chunk);
      stdoutTail = Buffer.concat([stdoutTail, chunk]);
      if (stdoutTail.length > MAX_OUTPUT_TAIL_BYTES) stdoutTail = stdoutTail.subarray(stdoutTail.length - MAX_OUTPUT_TAIL_BYTES);
    });
    child.stderr.on("data", (chunk) => {
      stderrStream.write(chunk);
      stderrTail = Buffer.concat([stderrTail, chunk]);
      if (stderrTail.length > MAX_ERROR_TAIL_BYTES) stderrTail = stderrTail.subarray(stderrTail.length - MAX_ERROR_TAIL_BYTES);
    });
    const terminate = (reason) => {
      if (settled || timedOut || stopped) return;
      timedOut = reason === "timeout";
      stopped = reason === "stop";
      (0, _postExitStdioGuard.trySignalChild)(child, "SIGTERM");
      hardKillTimer = setTimeout(() => {
        if (!settled) (0, _postExitStdioGuard.trySignalChild)(child, "SIGKILL");
      }, HARD_KILL_DELAY_MS);
      hardKillTimer.unref?.();
    };
    input.registerTimeout?.(() => terminate("timeout"));
    input.registerStop?.(() => terminate("stop"));
    child.stdin.on("error", () => {});
    child.stdin.end(input.prompt);
    let spawnError;
    child.once("error", (error) => {spawnError = error;});
    child.once("close", (exitCode, signal) => {
      settled = true;
      if (hardKillTimer) clearTimeout(hardKillTimer);
      input.registerTimeout?.(undefined);
      input.registerStop?.(undefined);
      const endedAt = Date.now();
      const externalProcess = {
        ...initialProcess,
        endedAt,
        durationMs: endedAt - startedAt,
        exitCode,
        processSignal: signal
      };
      input.onProcess?.(externalProcess);
      const stderr = stderrTail.toString("utf-8").trim();
      const error = stopped ?
      input.stopMessage ?? "Subagent stopped by user." :
      timedOut ?
      input.timeoutMessage ?? "Subagent timed out." :
      spawnError?.message ?? (exitCode === 0 ? undefined : stderr || `External CLI exited with code ${exitCode}.`);
      const result = {
        output: stdoutTail.toString("utf-8").trim(),
        exitCode: timedOut || stopped || spawnError ? 1 : exitCode,
        ...(error ? { error } : {}),
        ...(timedOut ? { timedOut: true } : {}),
        ...(stopped ? { stopped: true } : {}),
        processSignal: signal,
        externalProcess
      };
      stdoutStream.end();
      stderrStream.end();
      void streamsFinished.then((streamResults) => {
        const streamFailure = streamResults.find((streamResult) => streamResult.status === "rejected");
        if (streamFailure?.status === "rejected") reject(streamFailure.reason);else
        resolve(result);
      });
    });
  });
} /* v9-10ef5a3928e58963 */
