{
  "name": "slice-reference",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Generates the shared slice test fixtures by running windows/src/pet.ts slice() for real.",
  "scripts": {
    "gen": "vitest run"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  }
}
