"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.LAUNCH_BINDING_PROJECTION_VERSION = exports.AGENT_DEFINITION_PROJECTION_VERSION = void 0;exports.agentDefinitionDigest = agentDefinitionDigest;exports.launchBindingDigest = launchBindingDigest;exports.projectAgentDefinition = projectAgentDefinition;exports.projectLaunchBinding = projectLaunchBinding;var _nodeCrypto = await jitiImport("node:crypto");
var fs = _interopRequireWildcard(await jitiImport("node:fs"));function _interopRequireWildcard(e, t) {if ("function" == typeof WeakMap) var r = new WeakMap(),n = new WeakMap();return (_interopRequireWildcard = function (e, t) {if (!t && e && e.__esModule) return e;var o,i,f = { __proto__: null, default: e };if (null === e || "object" != typeof e && "function" != typeof e) return f;if (o = t ? n : r) {if (o.has(e)) return o.get(e);o.set(e, f);}for (const t in e) "default" !== t && {}.hasOwnProperty.call(e, t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, t)) && (i.get || i.set) ? o(f, t, i) : f[t] = e[t]);return f;})(e, t);}


const AGENT_DEFINITION_PROJECTION_VERSION = exports.AGENT_DEFINITION_PROJECTION_VERSION = 1;
const LAUNCH_BINDING_PROJECTION_VERSION = exports.LAUNCH_BINDING_PROJECTION_VERSION = 1;

function stableJson(value) {
  if (Array.isArray(value)) return `[${value.map(stableJson).join(",")}]`;
  if (value && typeof value === "object") {
    return `{${Object.entries(value).
    filter(([, entry]) => entry !== undefined).
    sort(([a], [b]) => a.localeCompare(b)).
    map(([key, entry]) => `${JSON.stringify(key)}:${stableJson(entry)}`).
    join(",")}}`;
  }
  return JSON.stringify(value);
}

function sha256(value) {
  return (0, _nodeCrypto.createHash)("sha256").update(stableJson(value)).digest("hex");
}

function fileDigest(filePath) {
  try {
    return (0, _nodeCrypto.createHash)("sha256").update(fs.readFileSync(filePath)).digest("hex");
  } catch {
    return undefined;
  }
}

/** Public-safe, deterministic evidence for the parsed launch-affecting agent definition. */
function projectAgentDefinition(agent) {
  return {
    version: AGENT_DEFINITION_PROJECTION_VERSION,
    name: agent.name,
    localName: agent.localName,
    packageName: agent.packageName,
    filePath: agent.filePath,
    fileContentDigest: fileDigest(agent.filePath),
    runner: agent.runner,
    systemPrompt: agent.systemPrompt,
    systemPromptMode: agent.systemPromptMode,
    inheritProjectContext: agent.inheritProjectContext,
    inheritSkills: agent.inheritSkills,
    model: agent.model,
    fallbackModels: agent.fallbackModels,
    thinking: agent.thinking,
    tools: agent.tools,
    mcpDirectTools: agent.mcpDirectTools,
    extensions: agent.extensions,
    subagentOnlyExtensions: agent.subagentOnlyExtensions,
    skills: agent.skills,
    skillPath: agent.skillPath,
    output: agent.output,
    defaultReads: agent.defaultReads,
    defaultProgress: agent.defaultProgress,
    defaultContext: agent.defaultContext,
    defaultAsync: agent.defaultAsync,
    defaultTimeoutMs: agent.defaultTimeoutMs,
    defaultTurnBudget: agent.defaultTurnBudget,
    defaultAcceptance: agent.defaultAcceptance,
    acceptanceRole: agent.acceptanceRole,
    interactive: agent.interactive,
    maxSubagentDepth: agent.maxSubagentDepth,
    completionGuard: agent.completionGuard,
    toolBudget: agent.toolBudget,
    memory: agent.memory
  };
}

function agentDefinitionDigest(agent) {
  return sha256(projectAgentDefinition(agent));
}






















/** Canonical projection of the resolved inputs handed to the child. */
function projectLaunchBinding(input) {
  return {
    version: LAUNCH_BINDING_PROJECTION_VERSION,
    definitionDigest: input.definitionDigest,
    taskDigest: input.task === undefined ? undefined : sha256(input.task),
    // The ordered candidate set already contains each attempted model; keeping only
    // this set makes retries correlate to the same preflight binding.
    modelCandidates: input.modelCandidates,
    thinking: input.thinking,
    systemPromptDigest: input.systemPrompt === undefined || input.systemPrompt === null ? undefined : sha256(input.systemPrompt),
    systemPromptMode: input.systemPromptMode,
    inheritProjectContext: input.inheritProjectContext,
    inheritSkills: input.inheritSkills,
    skills: input.skills,
    tools: input.tools,
    extensions: input.extensions,
    subagentOnlyExtensions: input.subagentOnlyExtensions,
    mcpDirectTools: input.mcpDirectTools,
    outputPath: input.outputPath,
    outputMode: input.outputMode,
    structuredOutputSchema: input.structuredOutputSchema
  };
}

function launchBindingDigest(input) {
  return sha256(projectLaunchBinding(input));
} /* v9-b24f25978b42fc02 */
